version: '3.9'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5005:5005"
    develop:
      watch:
        - path: .
          action: sync
        - path: .
          action: rebuild
